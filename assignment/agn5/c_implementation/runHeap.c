#include <stdio.h>
#include <stdlib.h>
#include "heap.h"

int * readIn(){
    int buf = 30 ;
    char * in = (char *)malloc(sizeof(char)*buf);
    scanf("%d");
}
int main(){
    struct heap * h = initHeap();
    return 0 ;
}
